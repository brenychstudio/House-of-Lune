# BR-03 Catalog Implementation Plan

**Goal:** Canonical market-aware catalog with safe public projections and stable future presentation identifiers.

**Architecture:** Extend BR-02 domains and PostgreSQL repositories. Internal commands own transactions and audit receipts; a dedicated query service returns consistent public projections. Next.js calls a server-only catalog gateway at request time and retains an editorial fallback on failure.

**Spec:** `C:/Users/CONCEPT2048/Downloads/BRENYCH_BR_03_CATALOG_SPATIAL_READY_ULTRA_CODEX_SVCODE.md` (user-authorized executable BR-03 task).

**Constraints:** Stack on `bc72d5564066758f45b98fb4216e2115e990fb76`; no production operations; no renderer dependency; MASK 01 is DRAFT / UNDECIDED / NOT_FOR_SALE with no variant, price, edition, or capacity. Preserve migrations 0001–0007 and all BR-02 invariants.

## 1. Commercial model and commands

- [x] Add failing domain tests in `tests/unit/domain/catalogCommercial.test.ts`: activation, scarcity, finish identity, acquisition and lead-time validation.
- [x] Extend `src/modules/catalog/domain.ts`; add `commercial.ts` and `availability.ts` with pure validation/derivation.
- [x] Add `0008_catalog_commercial_profile.sql`: safe defaults, finish identity, nullable lead-time promise, edition configuration and stable identity constraints.
- [x] Add transactional `catalogService.ts` commands for product and variant lifecycle and edition definition with audit receipts. Prove them using real PostgreSQL in `tests/integration/catalog.test.ts`.

## 2. Effective-dated pricing

- [x] Test date/revision validation, market mapping, historical/future/no-price resolution, immutable published records and simultaneous overlapping publication.
- [x] Add `0009_pricing_effective_windows.sql`; use half-open ranges and a PostgreSQL GiST exclusion constraint, with immutable published books/entries. Retain legacy retired history without inventing dates.
- [x] Extend pricing repositories and add `pricingService.ts`. Publication never retires another book. Resolution rejects ambiguous results and returns explicit no-price.

## 3. Public projections and seed

- [x] Test exclusion of drafts/inactive variants, actual inventory/edition/capacity availability, missing promises, and stable IDs.
- [x] Add `src/modules/catalog/readModel.ts` and `src/platform/db/queries/publicCatalog.ts`; take a repeatable-read snapshot for each projection.
- [x] Keep draft identity access a separate development-only sanitized query. No general public include-drafts switch.
- [x] Replace the guarded seed's variant creation with exactly one idempotent noncommercial MASK 01 product. Verify the executable seed, including production denial.

## 4. Storefront and degradation

- [x] Add E2E tests before route changes: canonical identity/status, zero fabricated fields, no acquire action, accessible fallback on database outage.
- [x] Add `src/site/catalog/gateway.ts`, request-time object routes and semantic catalog components. Keep editorial content distinct from commercial state.
- [x] Verify both live local PostgreSQL and unavailable database paths. Build with no database configuration. Smoke OpenNext locally.

## 5. Evidence and closure

- [x] Update schema checker and prove forward/empty PostgreSQL 17 bootstrap and checksum consistency.
- [x] Record SPP read-only evidence and ADR 0009; preserve stable productId/productSlug/variantId/finishCode without a renderer.
- [x] Fresh npm ci/audit/lint/typecheck/unit/integration/E2E/build/db:check/diff gate plus scans.
Remote delivery follows the documentation closure commit: push the verified branch and create a Draft PR to the migration branch. The final execution receipt records remote status.

Execution is inline in the authorized workspace/stacked branch. Each behavior follows a red/green test cycle; no additional approval checkpoint is required by the executable task.
