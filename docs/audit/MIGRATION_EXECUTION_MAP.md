# Migration execution map

## Sequence

1. Preserve `main@ac86caf4dc4abeebcd0f02a4cdc7b7e9ce6580c1` with `house-of-lune-pre-brenych-20260905`.
2. Audit in detached worktree; capture baseline command and screenshot evidence.
3. Commit audit/canon evidence on `migration/br-00-02-brenych`.
4. Remove tracked `node_modules` from the index and establish deterministic tooling.
5. Replace legacy runtime identity/routes/assets with the typed BRENYCH foundation.
6. Add framework-independent commerce contracts through TDD.
7. Add PostgreSQL schema/migrations and prove constraints against an empty real database.
8. Add transactional services and prove concurrency, outbox, idempotency, and append-only history.
9. Run browser/OpenNext/full verification and publish the result packet.

## Keep in place

- Git repository and full history
- Next.js App Router and TypeScript project basis
- OpenNext configuration pattern
- `package-lock.json` as the deterministic dependency record

## Adapt

- root package/configuration, CSS/token concepts, locale validation, typed content pattern, metadata and Cloudflare configuration

## Rebuild

- public routes, shell, navigation, content contracts, SEO identity, product foundation, spatial boundary

## Remove

- tracked dependency output, House of Lune runtime identity, invented jewelry products/copy/media, procedural ring scene, no-op inquiry API, old motion/3D runtime dependencies

## Add

- test stack, environment/market foundations, modular commerce contracts, PostgreSQL migrations, application services, outbox/idempotency/audit, QA/status/result evidence

## Rollback

No history is rewritten. To inspect or recover the exact source baseline, detach or branch from `house-of-lune-pre-brenych-20260905`. The migration branch is never merged automatically.

